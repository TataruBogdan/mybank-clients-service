

CREATE TABLE individual(
    id_serial text primary key,
    first_name text,
    last_name  text,
    address text,
    birth_date date,
    phone_number text,
    email_address text,
    cnp text,
    employer_name text,
    occupation text
);

